import Image from "next/image";

export default function CommissionModal({ id }: { id: Number }) {
  return (
    <div className="outline-4 bg-red outline-lightest-grey w-full h-full">
      <p>hiya</p>
    </div>
  );
}
