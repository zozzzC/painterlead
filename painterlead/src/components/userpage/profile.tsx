export type userInfo = { 
    username: String,
    pfpURL: String 
}

export default function profile(userInfo : userInfo) { 
    const { username, pfpURL }  = userInfo;

    return ( 
        <div>
            
        </div>
    );

}