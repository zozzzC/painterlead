export async function uploadImage({userId} : {userId: number}) {
    
}

export async function getImagesByUser({ userId }: { userId: number }) {}

export async function getImage({
    userId,
    imageId,
}: {
    userId: number;
    imageId: number;
}) {}

export async function deleteImage({userId} : {userId: number}) { 

}